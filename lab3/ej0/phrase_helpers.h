#ifndef _PHRASE_HELPERS_H
#define _PHRASE_HELPERS_H

void phrase_rebuild(unsigned int indexes[], char letters[],
					char sorted[], unsigned int length);
	// The sorted array if filled with each element in letters
	// In the position indicated by the indexes array

#endif
